import { PlayerAchievementProgress } from './player-achievement-progress';

describe('PlayerAchievementProgress', () => {
  it('should create an instance', () => {
    expect(new PlayerAchievementProgress()).toBeTruthy();
  });
});
