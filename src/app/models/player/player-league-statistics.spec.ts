import { PlayerLeagueStatistics } from './player-league-statistics';

describe('PlayerLeagueStatistics', () => {
  it('should create an instance', () => {
    expect(new PlayerLeagueStatistics()).toBeTruthy();
  });
});
