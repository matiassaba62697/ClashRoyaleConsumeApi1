import { PlayerAchievementBadge } from './player-achievement-badge';

describe('PlayerAchievementBadge', () => {
  it('should create an instance', () => {
    expect(new PlayerAchievementBadge()).toBeTruthy();
  });
});
